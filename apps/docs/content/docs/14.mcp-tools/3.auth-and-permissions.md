---
title: 'Auth and permissions'
description: 'Keep MCP tool visibility separate from the Convex authorization that actually decides the write or read.'
links:
  - label: MCP getting started
    icon: i-lucide-bot
    to: /docs/mcp-tools/getting-started
  - label: Destructive tools
    icon: i-lucide-triangle-alert
    to: /docs/mcp-tools/destructive-tools
---

MCP has two checks that are easy to mix up.

Tool visibility decides whether an agent can see or call a tool. Convex authorization decides whether the requested work is allowed.

Keep both. Do not let the tool layer become a second copy of your product rules.

| Layer                      | Owns                                   | Failure mode                           |
| -------------------------- | -------------------------------------- | -------------------------------------- |
| Transport (MCP middleware) | Who the bearer token represents        | No identity → no caller                |
| RecordAccess snapshot      | Coarse tool visibility and callability | Tool hidden or pre-rejected            |
| Backend authorization      | The actual product decision            | Same lane / `authorize` path as the UI |

## Transport identity first

MCP requests resolve a transport-shaped [caller](/docs/concepts/glossary#caller) — usually an `agent` caller derived from the validated bearer token and middleware context.

That's transport identity. It tells the runtime _how_ the call arrived. It doesn't decide what the caller can do.

::warning
Public MCP means deliberately public and read-only. Scoped workspace tools, writes, destructive tools, session mutation, and code-mode endpoints require bearer authentication.
::

## RecordAccess snapshot second

`defineMcpApp(...)` computes a recordAccess snapshot for coarse tool gating:

- Whether the tool should appear in the agent's tool list.
- Whether the tool should be callable at all.

That keeps the tool list predictable. If the agent does not have recordAccess, the tool is hidden or pre-rejected. That is still only the outer layer.

::note
Trellis now re-checks MCP `permission` and `enabled(...)` at execution time as well. Discovery still shapes the tool list, but execution does not trust session-time visibility alone.
::

## Business authorization still lives in Convex

This is the rule that keeps the system honest:

::warning
**Tool recordAccess checks don't replace backend authorization.** The same backend handler the UI calls is the handler the MCP tool calls. Caller checks, tenant isolation, permission or custom-guard checks, and authorize phases all still run.
::

Trellis examples derive coarse MCP recordAccess, then still call Convex handlers. Those handlers enforce the real rules through callers, lanes, permissions or custom guards, isolation, and `authorize`.

## Why the split

| Without the split                               | With the split                              |
| ----------------------------------------------- | ------------------------------------------- |
| Tool layer accumulates product policy over time | Tool layer keeps only coarse visibility     |
| Agent path drifts from UI path                  | Agent and UI call the same backend handlers |
| Two models to keep in sync                      | One backend model to test                   |

The browser and the agent end up on the _same_ authorization system. That's the design goal.

## Common pitfalls

- Treating recordAccess visibility as the final authorization decision.
- Letting the MCP tool layer grow its own business-policy branches instead of calling the backend handler.
- Forgetting that execution-time permission checks still matter even if discovery hid the tool correctly.

## What's next

- [Destructive tools](/docs/mcp-tools/destructive-tools) when you need preview + confirmation for dangerous actions.
- [Caller and appIdentity](/docs/permissions/caller-and-app-identity) for the identity model MCP principals plug into.
