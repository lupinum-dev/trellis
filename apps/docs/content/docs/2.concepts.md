---
title: 'Concepts'
description: 'The deeper mental model behind Trellis. Read this after the first task guides, then come back whenever a term stops making sense.'
---

These pages are not the day-1 path.

Use them after the first getting-started guides click, or when a Trellis term starts feeling fuzzy and you want the exact model behind it.

Three pages. One carries the mental model, one explains where rate limits belong, and one pins down the vocabulary. Every other guide links back here when a term stops making sense.

::card-group

::card{title="How it works" icon="i-lucide-waypoints" to="/docs/concepts/how-it-works"}
One protected business layer, four transports, one authorization path. The diagram version of the Trellis pitch.
::

::card{title="Rate-limiting layers" icon="i-lucide-gauge" to="/docs/concepts/rate-limiting-layers"}
Which limiter belongs where: edge, Better Auth, Trellis MCP, and app-owned Convex quotas.
::

::card{title="Glossary" icon="i-lucide-book-a" to="/docs/concepts/glossary"}
Canonical one-line definitions for every Trellis-specific term — caller, appIdentity, guard, operation, tenant, projection, and the rest.
::

::

## What's next

If you have not built a first app flow yet, go back to [Getting started](/docs/getting-started). If you already know what you are building, jump to the guide that matches: [Data fetching](/docs/data-fetching/queries), [Mutations](/docs/mutations/mutations), or [Permissions](/docs/permissions/setup).
