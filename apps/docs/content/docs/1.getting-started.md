---
title: 'Getting started'
description: 'Install Trellis, reach a first visible live update, and then add auth only when you actually need it.'
---

Trellis earns its complexity piece by piece. Start with the smallest path that proves the boundary you need — then add layers when the app asks for them.

::card-group

::card{title="Start here" icon="i-lucide-compass" to="/docs/getting-started/start-here"}
What Trellis is, what it isn't, and which starter baseline matches the app you're building.
::

::card{title="Installation" icon="i-lucide-download" to="/docs/getting-started/installation"}
Wire the module into a Nuxt app, set environment variables, and verify the baseline is healthy.
::

::card{title="First live query" icon="i-lucide-database" to="/docs/getting-started/first-live-query"}
The smallest useful Trellis app: one schema, one query, one page, one visible live update. ~15 minutes.
::

::card{title="Build a signed-in todo app" icon="i-lucide-lock" to="/docs/getting-started/build-a-signed-in-todo-app"}
Add auth, one protected query, and one protected mutation — without introducing tenancy or MCP yet.
::

::

## The path

Work through the cards top to bottom. If you already have a Nuxt + Convex app running and you just want to see Trellis click, skip straight to [First live query](/docs/getting-started/first-live-query).
