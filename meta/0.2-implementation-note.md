# Trellis 0.2 Implementation Note

Status: Active implementation
Date: 2026-06-03

This note captures the current hard-cut drift discovered during the `0.2 Operation Ladder`
implementation. The authoritative tracker remains [0.2 workpackages](./0.2-workpackages.md).

## Current Beginner Path

The current working beginner path is:

1. install the Nuxt module through `@lupinum/trellis`;
2. author app backend behavior through `@lupinum/trellis/app`;
3. define operations with `operation.query(...)`, `operation.mutation(...)`, and
   `operation.destructive(...)`;
4. use `workspaceScope()` for workspace operations;
5. write visible Convex filters and writes with `ctx.workspaceId`.

The lower-level backend APIs still exist for advanced use, but they should not be taught as a
parallel first path in beginner docs, starter fixtures, or templates.

## Resolved Drift

| Drift                                                      | Proof                                                                                                             |
| ---------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| Beginner docs taught backend-barrel root app setup.        | Starters, examples, and beginner docs import `defineTrellis` from `@lupinum/trellis/app`; source policy gates it. |
| Beginner operation docs taught `defineOperation(...)`.     | Beginner/reference pages teach `operation.*`; source policy gates the app-first function reference.               |
| Public access context was easy to model as `query.public`. | Maintained examples/starters/docs use `query.session`; source policy rejects public access-context handlers.      |
| Generated app tests copied raw forwarding/identity calls.  | Test helpers expose `asUser`, `asAuthUser`, `asService`, and `asCaller`; example-test source policy gates drift.  |
| Starter/generated tenant writes copied app identity.       | Workspace fixtures and generated resource operations use `workspaceScope()` and `ctx.workspaceId`.                |
| MCP operation refs were taught from the backend barrel.    | MCP docs/examples/generators import operation ref helpers from `@lupinum/trellis/mcp`; source policy gates it.    |

## Remaining Drift Mapped To Workpackages

| Drift                                                                                                      | Target phase       |
| ---------------------------------------------------------------------------------------------------------- | ------------------ |
| Auth bootstrap behavior is still partly app-owned in templates and consumers.                              | Phase 5            |
| MCP generated slices still need hardening around async context, bearer auth, and real tool-call readiness. | Phase 6            |
| MCP Convex caller setup still has app-specific caller-shape glue in examples/templates.                    | Phase 7            |
| Permission explanation and matrix commands are not yet the canonical debugging path.                       | Phase 8            |
| Preset output still needs proof that it is mechanically equivalent to ladder commands.                     | Phase 9            |
| Ginko and the shadcn Trellis template still need consumer smoke validation against the 0.2 shape.          | Phase 10, Phase 11 |

## Verified In This Slice

- `@lupinum/trellis/app` exports the beginner operation API.
- `workspaceScope()` injects only `ctx.workspaceId`; it does not add scoped DB wrappers.
- Public and personal starter fixtures define todo behavior through `operation.query(...)` and
  `operation.mutation(...)`.
- Canonical workspace starter fixtures use explicit `ctx.workspaceId` in reads and writes.
- Generated public, personal/auth, workspace, and workspace-MCP starter shapes typecheck.
- Generic resource generation emits `operation.*` templates from
  `@lupinum/trellis/app`; MCP-enabled workspace resource operations use
  `workspaceScope()` and `ctx.workspaceId` instead of hand-copying
  `appIdentity.workspaceId`.
- Maintained examples and starter fixtures use `@lupinum/trellis/app` for root
  `defineTrellis` setup.
- Access context handlers use `query.session(...)`; production-copyable source
  policy rejects `query.public(defineAccessContext(...))`.
- MCP tool authoring imports operation ref helpers from `@lupinum/trellis/mcp`,
  not from the backend barrel.

## Verification Evidence

- `pnpm vitest run --project=unit tests/unit/app-index-exports.test.ts tests/unit/package-subpath-exports.test.ts tests/unit/phase0-starter-manifest.test.ts`
- `pnpm exec tsc -p tsconfig.types.public.json --noEmit`
- `pnpm exec eslint src/runtime/app tests/unit/app-index-exports.test.ts tests/dts/app.types.ts src/cli/starter-fixtures/workspace/convex/features/todos/domain.ts src/cli/starter-fixtures/workspace-mcp/convex/features/todos/domain.ts tests/unit/phase0-starter-manifest.test.ts`
- `PATH=<pnpm-wrapper-with-minimumReleaseAge-0> pnpm run check:starter-fixtures:typecheck`

The pnpm wrapper is currently needed because the workspace package-manager policy blocks fresh Nuxt
releases during generated starter installs unless `minimumReleaseAge=0` is supplied.
